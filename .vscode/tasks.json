{
    "version": "2.0.0",
    "tasks": [{
        "label": "clear_shit",
        "command": "(mkdir ${workspaceFolder}/work || true) && rm -rf ${workspaceFolder}/work/*", // Could be any other shell command
        "args": [],
        "type": "shell"
    }]
}
